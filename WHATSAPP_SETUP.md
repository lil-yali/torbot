# מדריך: מספר וואטסאפ אמיתי (יציאה מה-Sandbox)

המטרה: שלקוחות אמיתיים יוכלו פשוט לכתוב למספר של העסק ולהזמין תור — בלי שלב ה-`join` של ה-Sandbox.

**מה זה דורש:** רישום **WhatsApp Sender** אמיתי ב-Twilio, שמחובר לחשבון **Meta Business** מאומת.
**כמה זמן:** בדרך כלל שעות עד כמה ימים (בגלל אישור השם והאימות של Meta).
**עלות (בערך — בדוק תמחור עדכני):** שכירת מספר ב-Twilio (~$1–15 לחודש) + תשלום פר-שיחה של WhatsApp. שיחות שהלקוח יוזם (כמו בהזמנת תור) — יש כ-1,000 שיחות בחודש בחינם, כך שלפיילוט קטן העלות זניחה.

---

## מה צריך להכין מראש

1. **חשבון Meta Business (Facebook Business Manager)** — https://business.facebook.com
2. **מספר טלפון** שיהיה מספר הוואטסאפ של העסק:
   - יכול להיות מספר שאתה קונה מ-Twilio, או מספר שבבעלותך.
   - **חשוב:** המספר לא יכול להיות רשום כבר ב-WhatsApp רגיל. אם הוא רשום — צריך למחוק אותו מ-WhatsApp קודם.
   - צריך גישה לקבל קוד אימות (SMS/שיחה) למספר.
3. **שם תצוגה לעסק** (Display Name) — Meta בודקת ומאשרת אותו (למשל "מספרת TorBot").

---

## שלב 1 — רישום ה-Sender ב-Twilio

1. היכנס ל-Twilio Console → **Messaging → Senders → WhatsApp senders**.
2. לחץ **Create new sender** (או "Sign up for WhatsApp").
3. Twilio יפתח תהליך מובנה שמחבר את **חשבון ה-Meta Business** שלך — עקוב אחרי השלבים:
   - התחבר/צור חשבון Meta Business.
   - בחר/הוסף את מספר הטלפון של העסק, ואמת אותו עם הקוד שתקבל.
   - הזן את **שם התצוגה** של העסק.
4. שלח לאישור. Meta תבדוק את שם התצוגה (יכול לקחת מרגעים עד יום-יומיים).

## שלב 2 — אימות עסק ב-Meta (Business Verification)

- לנפח נמוך אפשר לרוב להתחיל לשלוח מיד (עם מגבלות).
- לנפח גבוה / הסרת מגבלות — Meta דורשת **Business Verification** (מסמכי עסק). זה נעשה ב-Meta Business Settings → Security Center.

## שלב 3 — לחבר את המספר החדש למערכת

1. ב-Twilio, בהגדרות ה-Sender, קבע את ה-**webhook** של הודעות נכנסות:
   `https://torbot-yxjg.onrender.com/webhook` (Method: POST).
   *(אותו endpoint כמו ה-Sandbox — הוא כבר יודע לנתב לפי המספר.)*
2. ב-Render → Environment, עדכן:
   - `TWILIO_WHATSAPP_FROM` = `whatsapp:+<המספר-החדש>`
   - **לעסק יחיד (פיילוט):** השאר `SANDBOX_BUSINESS_PHONE` על מספר העסק — הכי פשוט.
   - **לכמה עסקים:** מחק את `SANDBOX_BUSINESS_PHONE`, ואז לכל עסק תגדיר בהגדרות האתר את "מספר הוואטסאפ העסקי" = מספר ה-Sender שלו (המערכת מנתבת אוטומטית לפי המספר).
   - (מומלץ לאבטחה) `VALIDATE_TWILIO` = `true` ו-`PUBLIC_WEBHOOK_URL` = `https://torbot-yxjg.onrender.com/webhook`.
3. שמור (Render יפרוס מחדש).

## שלב 4 — תבניות הודעה (רק אם רוצים תזכורות)

הודעה שהעסק **יוזם** מחוץ לחלון של 24 שעות (כמו תזכורת יום לפני) דורשת **תבנית מאושרת**:
1. ב-Twilio → **Content Template Builder**, צור תבנית בעברית עם משתנים (למשל: "היי {{1}}, תזכורת לתור שלך ב-{{2}}").
2. שלח לאישור Meta.
3. אחרי אישור, קח את ה-**Content SID** (מתחיל ב-`HX...`) ושים ב-Render:
   `REMINDER_TEMPLATE_SID` = `HX...` וגם `ENABLE_REMINDERS` = `true`.

*(בלי תבנית — שיחות רגילות עובדות מצוין בתוך 24 שעות מהודעת הלקוח, וזה מכסה כמעט את כל תהליך ההזמנה.)*

---

## סיכום זרימה
לקוח כותב למספר העסק → Twilio → `/webhook` → הבוט מנתב לעסק לפי המספר → עונה בוואטסאפ. אין שלב `join`, חוויה מלאה. ✅
